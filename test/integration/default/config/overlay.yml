#@ load("@ytt:overlay", "overlay")

#@overlay/match by=overlay.subset({"metadata":{"name":"opentelemetry-operator"}, "kind":"PackageInstall"})
---
spec:
  #@overlay/match missing_ok=True
  values:
    - secretRef:
        name: opentelemetry-operator-values
